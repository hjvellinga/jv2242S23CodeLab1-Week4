using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class HeartsParticleScript : MonoBehaviour
{
  //  public ParticleSystem heartsParticleSystem; //declare hearts particle system
   // GameObject gameManager; 
  //  public GameObject myManager;
    // Start is called before the first frame update
    void Start()
    {
    //    CollisionScript.FindManager();
    }

    // Update is called once per frame
    void Update()
    {
     //   if (myManager.playersCollided = true)
        {
            
        }

    }

  //  private void OnCollisionEnter(Collision collision)
   // {
  //      if (collision.transform.tag == "Player")
  //      {
  //          
  //      }
  //  }
 
}
